# Dependencies for the AgentHandler Docker image only.
# megadesk-contracts is installed from the worktree by the Dockerfile, not listed
# here, because it is a path dependency rather than something on an index.
cursor-sdk>=0.1.0
redis>=5.0.0
langgraph>=1.2.11,<2
