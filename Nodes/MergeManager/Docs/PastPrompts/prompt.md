We are making a GUI for a merge manager for agentic commits. 

Build a GUI using @markup.png as a reference. Use Dear PyGUI. 

Write program to scan the REDIS database at localhost for the first stream matching the FINISHED:* pattern. For each item create a ticket body with 
3 Buttons for 3 levels of verification: test me!, open in vscode (faster launch), cursor (lowest but most powerfuil IDE). 

Cursor to have a TESTME.md on the worktree that expains how to launch. Pressing will make an agent execute instructions. 
Opens the work tree in vscode. 
Opens worktree in cursor. NOT THE CURSOR AGENTS WINDOW, THE CURSOR IDE!

The testme button can unimplemented for now. 

