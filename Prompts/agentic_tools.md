<context>

We want to add an agentic overhaul so that VoiceDeck can operate anything on MegaDesk-Canvas that a human would be able to operate. The same way that voice deck currently can manipulate the CodeScope and agents should be extended so that every node has a set of accessible tools. Should this be an MCP server?

</context> 

