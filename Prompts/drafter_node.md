<context>

We want to make a new node whose job is to take notes. We want a bunch of different tabs representing each note and then body text. These files should be saved. We want VoiceDeck to be able to control these, so it needs to have a publicly available tool set.

</context>