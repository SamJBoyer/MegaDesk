# Bytecode / caches
__pycache__/
*.py[cod]
*$py.class
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.cache/

# Virtual environments
.venv/
venv/
ENV/
env/

# Packaging / build
build/
dist/
*.egg-info/
*.egg
.eggs/
pip-wheel-metadata/
*.whl

# Environment / secrets
.env
.env.*
!.env.example

# IDE / editor
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~

# OS
Thumbs.db
Desktop.ini
.DS_Store
ehthumbs.db

# Logs / local dumps
*.log
*.tmp
*.bak

# Supervisor session transcripts (keep the README so the folder is findable)
Logs/*
!Logs/README.md

# Last-open graph pointer (local preference; Graphs/*.json stay tracked)
Graphs/CURRENT

# MachineFactory Floor worktrees (bare clones + checkouts)
Nodes/Factory/MachineFactory/Floor/

# CodeScope disposable clones (never share with a writing agent)
Nodes/CodeScope/Scope/

# PRManager disposable PR checkouts (override with PR_SCOPE_ROOT)
Nodes/PRManager/Scope/

# Notepad clones and local scratch (notes inside a GIT_URL clone are the real files)
Nodes/Notepad/Scope/
Nodes/Notepad/notes/

# Local clone of the public smoke-test repo (not part of MegaDesk)
SMOKETESTREPO/

# Integration-test screenshots and failure artifacts
tests/_artifacts/

# Docker / local runtime leftovers
.docker/
